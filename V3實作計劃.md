# Astro SSG 遷移實作計劃

本計劃採用 **獨立 Repository + 純 SSG (Static Site Generation)** 策略,將 `apps_download_web` 遷移至 Astro 框架,並部署至 GitHub Pages。

---

## 使用者確認事項

> [!IMPORTANT]
> **已確認的技術選型**
>
> - ✅ **框架**: Astro (輕量、零 JavaScript、SEO 友善)
> - ✅ **渲染模式**: 純 SSG (Static Site Generation)
> - ✅ **部署策略**: 獨立 Repository (`apps_download_web_v3`)
> - ✅ **託管平台**: GitHub Pages
> - ✅ **資料獲取**: Build Time (無需 Runtime Server)

> [!NOTE]
> **為何選擇純 SSG?**
>
> GitHub Pages 僅支援靜態檔案,不支援 Node.js Runtime。使用純 SSG:
>
> - ✅ 完全相容 GitHub Pages
> - ✅ 極致效能 (無伺服器渲染延遲)
> - ✅ 簡化部署流程
> - ✅ 資料在 Build Time 預先生成
>
> **資料來源**: 目前的 `api/versions.json` 將在建置時讀取並預渲染到 HTML 中

---

## 總體架構

```
┌─────────────────────────────────────────────────────────────┐
│ 現有專案 (v2)                                                │
│ Repository: apps_download_web                               │
│ URL: https://presentyourlove.github.io/apps_download_web/  │
│ 狀態: 持續線上運作                                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 並行開發
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 新專案 (v3)                                                  │
│ Repository: apps_download_web_v3                            │
│ URL: https://presentyourlove.github.io/apps_download_web_v3/│
│ 技術: Astro SSG + GitHub Actions                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 驗證完成後
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 切換策略                                                     │
│ 選項 1: 保留兩個 repo,DNS 切換至 v3                         │
│ 選項 2: 將 v3 內容遷移回 apps_download_web                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 提議變更內容

### 階段一:專案初始化

#### 1.1 建立新 GitHub Repository

**步驟**:

1. 前往 GitHub,建立新 repository `apps_download_web_v3`
2. **不要** Initialize with README (稍後會透過 Astro 初始化)

**設定**:

```
Repository name: apps_download_web_v3
Description: Astro-powered app download center (v3)
Visibility: Public
```

---

#### 1.2 本地初始化 Astro 專案

**執行指令**:

```powershell
# 1. 建立 Astro 專案
npm create astro@latest apps_download_web_v3

# 提示選項:
# - Template: Empty
# - Install dependencies: Yes
# - TypeScript: Yes (Strict)
# - Initialize git: Yes

# 2. 進入專案目錄
cd apps_download_web_v3

# 3. 安裝額外依賴
npm install @astrojs/sitemap -D
```

---

#### 1.3 基礎配置檔案

#### [NEW] astro.config.mjs

```javascript
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://presentyourlove.github.io',
  base: '/apps_download_web_v3',
  output: 'static', // 純 SSG
  
  integrations: [
    sitemap()
  ],
  
  build: {
    assets: '_astro',
    inlineStylesheets: 'auto'
  },
  
  vite: {
    build: {
      cssMinify: 'lightningcss'
    }
  }
});
```

**關鍵設定說明**:

- `base`: GitHub Pages 子路徑
- `output: 'static'`: 純靜態生成
- `sitemap()`: 自動生成 sitemap.xml

---

#### [NEW] tsconfig.json

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react"
  }
}
```

---

### 階段二:資料層建置

#### 2.1 複製資料檔案

**步驟**:

```powershell
# 從舊專案複製資料檔案
mkdir -p public/api
cp ../apps_download_web/api/versions.json public/api/
```

---

#### 2.2 建立資料存取層

#### [NEW] src/lib/data.ts

建立型別安全的資料存取函式。

```typescript
// 型別定義
export interface Platform {
  type: string;
  version: string;
  downloadUrl?: string;
  storeUrl?: string;
  minSdk?: number;
  minOS?: string;
  size?: string;
  url?: string;
}

export interface Changelog {
  version: string;
  date: string;
  changes: string[];
}

export interface App {
  id: string;
  name: string;
  displayName: string;
  version: string;
  releaseDate: string;
  platforms: Record<string, Omit<Platform, 'type'>>;
  changelog: Changelog[];
}

export interface AppsData {
  lastUpdated: string;
  apps: App[];
}

// 資料獲取函式 (Build Time)
export async function getAppsData(): Promise<AppsData> {
  const response = await fetch(
    new URL('../../public/api/versions.json', import.meta.url)
  );
  return response.json();
}

// 輔助函式:將 platforms 物件轉為陣列
export function getPlatformsArray(app: App): Platform[] {
  return Object.entries(app.platforms).map(([type, details]) => ({
    type,
    ...details
  }));
}

// 輔助函式:依 ID 查詢應用程式
export function getAppById(apps: App[], id: string): App | undefined {
  return apps.find(app => app.id === id);
}
```

**重點**:

- ✅ Build Time 讀取資料 (無需 Runtime API)
- ✅ TypeScript 型別安全
- ✅ 輔助函式方便使用

---

### 階段三:版面與元件

#### 3.1 基礎版面配置

#### [NEW] src/layouts/BaseLayout.astro

```astro
---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { 
  title, 
  description = 'Presentyourlove 應用程式下載中心',
  ogImage = '/assets/presentyourlove-logo-512.png'
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL(ogImage, Astro.site);
---

<!DOCTYPE html>
<html lang="zh-TW" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
  <meta name="description" content={description}>
  
  <!-- SEO -->
  <link rel="canonical" href={canonicalURL}>
  
  <!-- Open Graph -->
  <meta property="og:type" content="website">
  <meta property="og:url" content={canonicalURL}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={ogImageURL}>
  
  <!-- PWA -->
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#0f172a">
  
  <!-- Icons -->
  <link rel="icon" href="/assets/presentyourlove-logo-32.png" sizes="32x32">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
  <div id="header-placeholder"></div>
  
  <main id="main-content">
    <slot />
  </main>
  
  <div id="footer-placeholder"></div>
  
  <script>
    // Theme initialization
    const theme = localStorage.getItem('theme') || 'dark';
    document.documentElement.setAttribute('data-theme', theme);
  </script>
</body>
</html>
```

**特點**:

- ✅ 完整的 SEO meta tags
- ✅ Open Graph 支援
- ✅ 型別安全的 Props
- ✅ 自動生成 canonical URL

---

#### 3.2 可重用元件

#### [NEW] src/components/AppCard.astro

```astro
---
interface Props {
  id: string;
  name: string;
  displayName: string;
  iconPath: string;
}

const { id, name, displayName, iconPath } = Astro.props;
const detailUrl = `${import.meta.env.BASE_URL}${id}/`;
---

<a href={detailUrl} class="app-card">
  <div class="app-card-icon">
    <img 
      src={iconPath} 
      alt={`${name} Logo`}
      width="80" 
      height="80"
      loading="lazy"
    />
  </div>
  <h2>{name}</h2>
  <p>{displayName}</p>
  <span class="app-card-arrow">→</span>
</a>

<style>
  .app-card {
    background: var(--card-bg);
    border: var(--glass-border);
    backdrop-filter: blur(12px);
    padding: 40px 32px;
    border-radius: 24px;
    text-align: center;
    text-decoration: none;
    color: var(--text-primary);
    transition: transform 0.3s ease;
  }
  
  .app-card:hover {
    transform: translateY(-4px);
  }
  
  /* ... 其他樣式 ... */
</style>
```

---

### 階段四:頁面建置

#### 4.1 首頁

#### [NEW] src/pages/index.astro

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import AppCard from '../components/AppCard.astro';
import { getAppsData } from '../lib/data';

// Build Time 獲取資料
const { apps } = await getAppsData();
---

<BaseLayout title="Presentyourlove - 應用程式下載中心">
  <section class="welcome-section">
    <div class="container">
      <h1 class="welcome-title text-gradient-animated">
        歡迎使用我們的應用程式
      </h1>
      <p class="welcome-desc">
        請選擇您想要下載的應用程式
      </p>
      
      <div class="app-grid">
        {apps.map(app => (
          <AppCard
            id={app.id}
            name={app.name}
            displayName={app.displayName}
            iconPath={`/assets/${app.id}-icon-192.png`}
          />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
```

**重點**:

- ✅ Build Time 預先渲染
- ✅ 無需客戶端 JavaScript
- ✅ SEO 完美

---

#### 4.2 應用程式詳細頁 (動態路由)

#### [NEW] src/pages/[appId].astro

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getAppsData, getAppById, getPlatformsArray } from '../lib/data';

// Build Time 生成所有頁面
export async function getStaticPaths() {
  const { apps } = await getAppsData();
  
  return apps.map(app => ({
    params: { appId: app.id },
    props: { app }
  }));
}

const { app } = Astro.props;
const platforms = getPlatformsArray(app);
---

<BaseLayout 
  title={`${app.displayName} - 下載 | Presentyourlove`}
  description={`下載 ${app.displayName} v${app.version},支援多平台安裝`}
>
  <article class="app-detail">
    <header class="app-header">
      <img 
        src={`/assets/${app.id}-icon-192.png`}
        alt={`${app.name} Icon`}
        width="120"
        height="120"
      />
      <div>
        <h1>{app.displayName}</h1>
        <p class="version">版本 {app.version}</p>
      </div>
    </header>
    
    <section class="platforms">
      <h2>支援平台</h2>
      <div class="platform-grid">
        {platforms.map(platform => (
          <div class="platform-card">
            <h3>{platform.type}</h3>
            <p>版本: {platform.version}</p>
            {platform.downloadUrl && (
              <a href={platform.downloadUrl} class="btn-download">
                立即下載
              </a>
            )}
          </div>
        ))}
      </div>
    </section>
    
    <section class="changelog">
      <h2>更新記錄</h2>
      {app.changelog.map(entry => (
        <div class="changelog-entry">
          <h3>v{entry.version} ({entry.date})</h3>
          <ul>
            {entry.changes.map(change => (
              <li>{change}</li>
            ))}
          </ul>
        </div>
      ))}
    </section>
  </article>
</BaseLayout>
```

**關鍵功能**:

- ✅ `getStaticPaths()`: 為每個應用程式生成靜態頁面
- ✅ 動態路由: `/financeapp`, `/subtrack`, `/sub-buddy`
- ✅ SEO 友善的結構化資料

---

### 階段五:樣式遷移

#### [NEW] src/styles/global.css

直接複製現有專案的 `css/style.css`,並調整路徑:

```css
/* 複製 apps_download_web/css/style.css 內容 */
/* 調整資源路徑使用相對路徑 */

:root {
  --font-primary: 'Inter', 'Noto Sans TC', system-ui, sans-serif;
  /* ... 其他 CSS Variables ... */
}

/* ... 完整樣式 ... */
```

---

### 階段六:靜態資源遷移

**複製檔案**:

```powershell
# 複製所有靜態資源
cp -r ../apps_download_web/assets public/
cp -r ../apps_download_web/components public/
cp ../apps_download_web/manifest.json public/
cp ../apps_download_web/humans.txt public/
cp ../apps_download_web/robots.txt public/
```

---

### 階段七:GitHub Actions 部署

#### [NEW] .github/workflows/deploy.yml

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build Astro site
        run: npm run build
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

## 驗證計劃

### 本地測試

```powershell
# 1. 開發模式
npm run dev
# 訪問 http://localhost:4321/apps_download_web_v3/

# 2. 建置測試
npm run build

# 3. 預覽建置結果
npm run preview
# 訪問 http://localhost:4321/apps_download_web_v3/
```

**檢查項目**:

- [ ] 所有頁面正常顯示
- [ ] 路徑正確 (包含 `/apps_download_web_v3/` base)
- [ ] 圖片正常載入
- [ ] 深色模式運作
- [ ] 連結正確導航

---

### GitHub Pages 測試

**部署後檢查**:

1. 訪問 `https://presentyourlove.github.io/apps_download_web_v3/`
2. 測試所有頁面
3. 檢查 Console 無錯誤
4. 驗證 SEO meta tags (使用瀏覽器開發者工具)
5. 執行 Lighthouse 測試

**預期 Lighthouse 分數**:

- Performance: 95+
- Accessibility: 100
- Best Practices: 100
- SEO: 100

---

## 切換上線策略

### 選項 1:DNS/網域切換 (推薦)

**適用情境**: 有自訂網域

**步驟**:

1. v3 上線後持續運作 1-2 週,確認穩定
2. 將網域 CNAME 指向 `apps_download_web_v3`
3. 舊專案保留作為備援

---

### 選項 2:Repository 內容替換

**步驟**:

```powershell
# 1. Clone v3 的 dist 目錄
cd apps_download_web_v3
npm run build

# 2. 將產物複製到原 repo
cd ../apps_download_web
rm -rf * (保留 .git)
cp -r ../apps_download_web_v3/dist/* .

# 3. 更新 base 路徑
# 需調整 astro.config.mjs 的 base 為 '/apps_download_web'
# 重新建置並提交

git add .
git commit -m "feat: migrate to Astro SSG"
git push
```

---

## 時程規劃

| 階段 | 工作內容 | 預估時間 |
|:-----|:---------|:---------|
| **階段一** | 專案初始化、Repository 建立 | 1hr |
| **階段二** | 資料層建置 | 1hr |
| **階段三** | 版面與元件開發 | 3hr |
| **階段四** | 頁面建置 (5 頁) | 5hr |
| **階段五** | 樣式遷移 | 2hr |
| **階段六** | 靜態資源遷移 | 1hr |
| **階段七** | GitHub Actions 設定 | 1hr |
| **測試驗證** | 本地 + 線上測試 | 4hr |
| **文件更新** | README、部署文件 | 2hr |
| **總計** | | **20hr** |

---

## 後續維護

### 更新應用程式資訊

**流程**:

1. 編輯 `public/api/versions.json`
2. Commit & Push
3. GitHub Actions 自動建置部署
4. 約 2-3 分鐘後更新生效

### 新增應用程式

**步驟**:

1. 在 `versions.json` 新增應用程式資料
2. 將應用程式 icon 放至 `public/assets/`
3. Astro 會自動生成新頁面 (透過 `getStaticPaths`)

---

**準備好開始實作了嗎?** 🚀
